#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

/*
  Although this problem is marked as easy, it took me a while to determine.
  I performed this brute force method but am not happy with it.  Would like
  to revisit.
*/
int main(){
    int n;
    cin >> n;
    for (int x = n; x >= 1; x--) {
    		for (int y = 1; y < i; y++) {
    			cout << " ";
    		}
    		for (int z = n; z >= i; z--) {
    			cout << "#";
    		}
		  cout << endl;
	  }
    return 0;
}
